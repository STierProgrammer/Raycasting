#pragma once

#define square(x)           ((x) * (x))
#define abs(x)              ((x) >= 0 ? (x) : -(x))
#define min(a, b)           ((a) > (b) ? (b) : (a))
#define max(a, b)           ((a) > (b) ? (a) : (b))

int gcd(int a, int b);


